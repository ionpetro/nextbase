# Installation

Nextbase is built on the [Next.js](https://nextjs.org/) framework. It is a React framework that allows for server-side rendering of React components. It also allows for static site generation, which is what we use for our documentation site.
